import 'package:flutter/material.dart';

class MapinPage extends StatelessWidget {
  static const String routeName = "mapin-page";
  const MapinPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}